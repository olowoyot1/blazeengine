'use client'; import {useTransition} from 'react'; import {decideLeave} from '@/lib/actions'; import {useRouter} from 'next/navigation'; export function LeaveAction({id}:{id:string}){const[p,start]=useTransition();const r=useRouter();return <span style={{display:'flex',gap:6}}><button className="btn green" disabled={p} onClick={()=>start(async()=>{await decideLeave(id,'APPROVED');r.refresh()})}>Approve</button><button className="btn danger" disabled={p} onClick={()=>start(async()=>{await decideLeave(id,'REJECTED');r.refresh()})}>Reject</button></span>}
