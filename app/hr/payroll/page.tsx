import Shell from '@/components/Shell';import {requireCap} from '@/lib/guard';import {payrollRuns} from '@/lib/queries';import {naira,fmtDateTime} from '@/lib/format';import {PayrollForm} from './PayrollForm';import {PayrollActions} from './PayrollActions';import Link from 'next/link';
export default async function Payroll(){const s=await requireCap('payroll.read','hr.payroll');const runs=await payrollRuns();return <Shell s={s} title="Payroll" kicker="HR payroll · CEO approval · staff disbursement"><div className="grid2"><div className="card"><h3>Create payroll run</h3><p className="muted small">Active employees are pulled from HR. HR prepares payroll; CEO must approve before any disbursement.</p>{['HR','ADMIN','SUPER_ADMIN'].includes(s.role)?<PayrollForm/>:<p className="muted">You have read-only payroll access.</p>}</div><div className="card"><h3>Control rule</h3><p><b>HR → CEO approval → Finance/HR disbursement.</b></p><p className="muted small">An approved payroll is not paid until an authorized user explicitly disburses it. Approval and disbursement are audited.</p></div></div><div className="card" style={{marginTop:15}}><div className="section-title"><h3>Payroll runs</h3><div><Link className="btn" href="/api/export?type=payroll&format=xls">Excel</Link> <Link className="btn" href="/api/export?type=payroll&format=pdf">PDF</Link></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Month</th><th>Employees</th><th>Gross</th><th>Deductions</th><th>Net</th><th>Status</th><th>Created</th><th>Action</th></tr></thead><tbody>{runs.map((r:any)=><tr key={r.id}><td><Link href={'/hr/payroll/'+r.id}>{r.payroll_month}</Link></td><td>{r.item_count}</td><td>{naira(r.total_gross)}</td><td>{naira(r.total_deductions)}</td><td>{naira(r.total_net)}</td><td>{r.status}</td><td>{fmtDateTime(r.created_at)}</td><td><PayrollActions id={r.id} status={r.status}/></td></tr>)}</tbody></table></div></div></Shell>}
